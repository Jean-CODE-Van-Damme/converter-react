const Header = ({ title = "" }) => {
  return (
    <>
      <header>
        <span>💵</span>
        <h1>{title}</h1>
        <span>💵</span>
      </header>
    </>
  );
};

export default Header;
